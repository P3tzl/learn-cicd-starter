name: cd

on:
  push:
    branches: [main]

jobs:
  Deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: check out code
        uses: actions/checkout@v6

      - name: Set up GO
        uses: actions/setup-go@v6

      - name: Build app
        run: scripts/buildprod.sh
